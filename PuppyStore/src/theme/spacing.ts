// Consistent spacing scale (4px base unit)
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
  xxxl: 64,
} as const;

// Common layout patterns
export const layout = {
  screenPadding: 16,
  cardPadding: 16,
  cardRadius: 12,
  buttonRadius: 12,
  inputRadius: 8,
  badgeRadius: 4,
  gap: {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
  },
} as const;
